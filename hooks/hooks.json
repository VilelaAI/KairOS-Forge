{
  "hooks": {
    "SessionStart": [
      {
        "matcher": "*",
        "hooks": [
          {
            "type": "command",
            "command": "echo '🔥 kairos-forge v0.8 ativo — 86 agentes (24 core + 41 verticais em 9 squads + 21 apoio em 7 squads) | skills: /kairos-forge:onboardar especificar validar rodar mobilizar revisar auditar auditar-seguranca evoluir | Para mobilizar paralelo: export CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS=1'"
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "FILE=$(jq -r '.tool_input.file_path // empty'); if echo \"$FILE\" | grep -qiE '(auth|login|senha|password|secret|token|jwt|crypto|\\.env|Dockerfile|\\.tf$|k8s|kubernetes|(^|[^a-z])iam([^a-z]|$))'; then echo '🔐 kairos-forge: arquivo sensível a segurança modificado. Rode /kairos-forge:revisar (Helena escala o squad de segurança) e considere /kairos-forge:analisar-ameacas antes do PR.'; elif echo \"$FILE\" | grep -qE '\\.(py|ts|tsx|js|jsx|go|rs|java)$' && ! echo \"$FILE\" | grep -qE '(test|spec|__tests__)'; then echo '💡 kairos-forge: arquivo de produção modificado. Lembre de chamar Ricardo (testes), rodar /kairos-forge:validar se houver SPEC e /kairos-forge:revisar antes do PR.'; fi"
          }
        ]
      }
    ]
  }
}
